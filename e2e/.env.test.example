# E2E Test Environment Variables

# Base URL for the application (change based on mode)
E2E_BASE_URL=http://localhost:8080

# Optional: Command to start the server (uncomment if needed)
# E2E_START_SERVER=cargo run -p web_service_standalone -- --port 8080 --data-dir /tmp/test-data

# Test credentials (if needed for your tests)
# TEST_API_KEY=your-test-key-here

# Test timeouts (optional)
# TEST_TIMEOUT=30000
# TEST_EXPECT_TIMEOUT=5000
